# 标签自动打标（Auto-Tag）实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 独立脚本对一个已建 PingCode 项目，扫工作项内容、词典锚定抽出 ~12 个领域标签、建标签、按关键词并发贴到命中工作项，幂等并出报告。

**Architecture:** 纯逻辑（抽词/匹配）放 `pingcode_tag_logic.js` 单测；标签 API 封装进 `pingcode_api.js`（端点已实测）；`pingcode_tag_apply.js` 做编排 CLI。

**Tech Stack:** Node 24（内置 `node:test`）、`pingcode_api.js`（fetch 封装）、`pingcode_tags.js`（行业词典）。

参照 spec：`docs/specs/2026-06-25-标签自动打标-design.md`。标签 API 已实测：建 `POST /v1/project/tags`、列 `GET /v1/project/tags?project_id=`、贴 `POST /v1/project/work_items/{id}/tags {tag_id}`、删 `DELETE /v1/project/tags/{id}`。

---

## 文件结构
| 文件 | 职责 |
|------|------|
| `scripts/pingcode_api.js` | 加 `createTag / listTags / addWorkItemTag` 封装 |
| `scripts/pingcode_tags.js` | 导出 `INDUSTRY_TERMS / INDUSTRY_DEFAULT` 供取词典 |
| `scripts/pingcode_tag_logic.js` | 纯逻辑：`extractTags / matchTags`（可单测） |
| `scripts/test_tag_logic.js` | 纯逻辑单测 |
| `scripts/pingcode_tag_apply.js` | 编排 CLI：认证→拉项目→抽→建→贴→报告 |
| `references/api.md` | 标签 API 文档 |

---

## Task 1: 标签 API 封装（pingcode_api.js）

**Files:**
- Modify: `scripts/pingcode_api.js`（在 `module.exports` 前加三个函数，并加入导出）

- [ ] **Step 1: 写三个封装函数**

在 `scripts/pingcode_api.js` 的 `module.exports` 之前加入：

````javascript
// ============================================================
// 标签 (work_item_tags) — 端点 2026-06-25 实测
// ============================================================
async function createTag(token, opts, baseUrl = DEFAULT_BASE) {
  /** opts: { name*, projectId*, color } */
  const body = { name: opts.name, project_id: opts.projectId };
  if (opts.color) body.color = opts.color;
  const resp = await fetch(`${baseUrl}/v1/project/tags`, { method: "POST", headers: H(token), body: JSON.stringify(body) });
  if (!resp.ok) throw new Error(`创建标签失败: ${await resp.text()}`);
  return resp.json();
}

async function listTags(token, projectId, baseUrl = DEFAULT_BASE) {
  const resp = await fetch(`${baseUrl}/v1/project/tags?project_id=${projectId}&page_size=200`, { headers: H(token) });
  if (!resp.ok) throw new Error(`查询标签失败: ${await resp.text()}`);
  return (await resp.json()).values || [];
}

async function addWorkItemTag(token, workItemId, tagId, baseUrl = DEFAULT_BASE) {
  /** 给工作项追加一个标签（幂等：已贴再贴不报错） */
  const resp = await fetch(`${baseUrl}/v1/project/work_items/${workItemId}/tags`, { method: "POST", headers: H(token), body: JSON.stringify({ tag_id: tagId }) });
  if (!resp.ok) throw new Error(`贴标签失败: ${await resp.text()}`);
  return resp.json();
}
````

- [ ] **Step 2: 加入导出**

把 `createTag, listTags, addWorkItemTag` 加进 `pingcode_api.js` 的 `module.exports`（追加到已有导出对象里）。

- [ ] **Step 3: 实跑冒烟（建→贴→删，daocloud-test）**

Run:
```bash
cd scripts && node -e "
const api=require('./pingcode_api.js');
(async()=>{
  const {token}=await api.getToken(process.env.PINGCODE_CLIENT_ID, process.env.PINGCODE_CLIENT_SECRET);
  const p=(await api.listProjects(token)).find(x=>x.identifier==='JLALED');
  const wi=(await api.listWorkItems(token,p.id))[0];
  const tag=await api.createTag(token,{name:'_smoke_'+Date.now(),projectId:p.id,color:'#1890ff'});
  await api.addWorkItemTag(token,wi.id,tag.id);
  const tags=await api.listTags(token,p.id);
  console.log('建标签ok', !!tag.id, '| 列表含新标签', tags.some(t=>t.id===tag.id));
  await fetch('https://open.pingcode.com/v1/project/tags/'+tag.id,{method:'DELETE',headers:{Authorization:'Bearer '+token}});
})();
"
```
Expected: `建标签ok true | 列表含新标签 true`，无报错。

- [ ] **Step 4: 提交**

```bash
git add scripts/pingcode_api.js
git commit -m "feat(tags): pingcode_api 加 createTag/listTags/addWorkItemTag 封装"
```

---

## Task 2: 纯逻辑 extractTags（词典锚定抽标签）

**Files:**
- Modify: `scripts/pingcode_tags.js`（导出 INDUSTRY_TERMS / INDUSTRY_DEFAULT）
- Create: `scripts/pingcode_tag_logic.js`
- Test: `scripts/test_tag_logic.js`

- [ ] **Step 1: 让 pingcode_tags.js 导出词典**

确认 `scripts/pingcode_tags.js` 的 `module.exports` 含 `INDUSTRY_TERMS` 与 `INDUSTRY_DEFAULT`；若没有则在其 `module.exports` 里补上这两个键（值即文件顶部已定义的同名常量）。

- [ ] **Step 2: 写失败测试（extractTags）**

Create `scripts/test_tag_logic.js`:

````javascript
const { test } = require('node:test');
const assert = require('node:assert');
const L = require('./pingcode_tag_logic.js');

const ITEMS = [
  { title: 'ADB自适应远光系统需求', description: '防眩目分区遮蔽' },
  { title: 'ADB分区点亮算法详细设计', description: '渐变过渡' },
  { title: '配光性能系统需求 (ECE R112)', description: '近光配光照度' },
  { title: '近光配光软件需求', description: '光型控制' },
  { title: 'UDS诊断服务软件需求', description: 'CAN通信' },
  { title: '功能安全系统需求', description: 'ASIL B 故障检测' },
];
const DICT = ['ADB', '配光', 'UDS', '功能安全', 'ASIL', 'EMC'];

test('extractTags 取频次≥min的词典词, 按频降序', () => {
  const tags = L.extractTags(ITEMS, DICT, { min: 2, max: 12 });
  assert.deepStrictEqual(tags, ['ADB', '配光']); // ADB×2, 配光×2; UDS/功能安全/ASIL×1<min, EMC×0
});

test('extractTags max 封顶', () => {
  const tags = L.extractTags(ITEMS, DICT, { min: 1, max: 2 });
  assert.strictEqual(tags.length, 2);
  assert.deepStrictEqual(tags, ['ADB', '配光']); // 频次并列时按词典序定序
});

test('extractTags 大小写无关 + 命中描述', () => {
  const tags = L.extractTags([{ title: 'adb test', description: 'has EMC inside' }], ['ADB', 'EMC'], { min: 1, max: 5 });
  assert.ok(tags.includes('ADB') && tags.includes('EMC'));
});
````

- [ ] **Step 3: 运行确认失败**

Run: `cd scripts && node --test test_tag_logic.js`
Expected: FAIL（`Cannot find module './pingcode_tag_logic.js'`）。

- [ ] **Step 4: 实现 pingcode_tag_logic.js（extractTags）**

Create `scripts/pingcode_tag_logic.js`:

````javascript
const norm = s => (s || '').toString().toLowerCase();
const hayOf = it => norm(it.title) + '  ' + norm(it.description);

// 词典锚定抽标签: dict 词在工作项 title+description 出现的工作项数 ≥min, 按频降序取前 max
function extractTags(workItems, dict, { min = 2, max = 12 } = {}) {
  const counted = dict.map(term => {
    const t = norm(term);
    const n = workItems.filter(w => hayOf(w).includes(t)).length;
    return { term, n };
  }).filter(x => x.n >= min);
  counted.sort((a, b) => b.n - a.n || dict.indexOf(a.term) - dict.indexOf(b.term));
  return counted.slice(0, max).map(x => x.term);
}

module.exports = { extractTags, norm, hayOf };
````

- [ ] **Step 5: 运行确认通过**

Run: `cd scripts && node --test test_tag_logic.js`
Expected: PASS（3 tests）。

- [ ] **Step 6: 提交**

```bash
git add scripts/pingcode_tag_logic.js scripts/test_tag_logic.js scripts/pingcode_tags.js
git commit -m "feat(tags): extractTags 词典锚定抽标签 + 单测"
```

---

## Task 3: 纯逻辑 matchTags（关键词匹配贴标）

**Files:**
- Modify: `scripts/pingcode_tag_logic.js`
- Modify: `scripts/test_tag_logic.js`

- [ ] **Step 1: 追加失败测试（matchTags）**

Append to `scripts/test_tag_logic.js`:

````javascript
test('matchTags 子串命中即贴, 可多标签', () => {
  const item = { title: 'ADB分区点亮算法详细设计', description: '配光过渡' };
  assert.deepStrictEqual(L.matchTags(item, ['ADB', '配光', 'UDS']).sort(), ['ADB', '配光']);
});

test('matchTags 短标签命中长文本(子串subsumes别名)', () => {
  // 标签 "ADB" 命中 "ADB自适应远光"; 标签 "配光" 命中 "近光配光"
  const item = { title: 'ADB自适应远光与近光配光', description: '' };
  assert.deepStrictEqual(L.matchTags(item, ['ADB', '配光']).sort(), ['ADB', '配光']);
});

test('matchTags 无命中返回空', () => {
  assert.deepStrictEqual(L.matchTags({ title: '项目立项', description: '可行性' }, ['ADB', 'EMC']), []);
});
````

> 注：子串匹配天然涵盖"短标签↔长表述"（ADB↔ADB自适应远光），故 MVP 不需要单独别名表（spec §5 别名诉求由子串匹配 subsume）。

- [ ] **Step 2: 运行确认失败**

Run: `cd scripts && node --test test_tag_logic.js`
Expected: FAIL（`L.matchTags is not a function`）。

- [ ] **Step 3: 实现 matchTags**

在 `scripts/pingcode_tag_logic.js` 加入（module.exports 前）：

````javascript
// 关键词匹配: 工作项 title|description 含某标签词(子串,大小写无关) → 贴该标签
function matchTags(item, tagList) {
  const hay = hayOf(item);
  return tagList.filter(tag => hay.includes(norm(tag)));
}
````

并更新导出：

````javascript
module.exports = { extractTags, matchTags, norm, hayOf };
````

- [ ] **Step 4: 运行确认通过**

Run: `cd scripts && node --test test_tag_logic.js`
Expected: PASS（6 tests）。

- [ ] **Step 5: 提交**

```bash
git add scripts/pingcode_tag_logic.js scripts/test_tag_logic.js
git commit -m "feat(tags): matchTags 关键词匹配(子串subsumes别名) + 单测"
```

---

## Task 4: 编排 CLI（pingcode_tag_apply.js）

**Files:**
- Create: `scripts/pingcode_tag_apply.js`

- [ ] **Step 1: 写编排脚本**

Create `scripts/pingcode_tag_apply.js`:

````javascript
/**
 * 标签自动打标 — 对已建项目扫内容抽标签并贴到工作项
 * 用法: node pingcode_tag_apply.js --client_id=.. --client_secret=.. --identifier=JLALED [--industry=汽车电子] [--min=2] [--max=12] [--dry]
 */
const api = require('./pingcode_api.js');
const L = require('./pingcode_tag_logic.js');
const tagsDict = require('./pingcode_tags.js');
const fs = require('fs');
const path = require('path');
const args = Object.fromEntries(process.argv.slice(2).map(a => a.replace(/^--/, '').split('=')));
const DRY = 'dry' in args;
const MIN = +(args.min || 2), MAX = +(args.max || 12);

// 行业模板「术语」行 → 词
function verticalTerms(industry) {
  if (!industry) return [];
  const f = path.join(__dirname, '..', 'references', 'verticals', industry + '.md');
  if (!fs.existsSync(f)) return [];
  const m = fs.readFileSync(f, 'utf8').match(/-\s*术语\s*[:：]\s*(.+)/);
  return m ? m[1].split(/[,，、]/).map(s => s.trim()).filter(Boolean) : [];
}

function buildDictionary(industry) {
  const ind = (tagsDict.INDUSTRY_TERMS && tagsDict.INDUSTRY_TERMS[industry]) || [];
  const def = tagsDict.INDUSTRY_DEFAULT || [];
  return [...new Set([...verticalTerms(industry), ...ind, ...def])];
}

(async () => {
  if (!args.client_id || !args.client_secret || !args.identifier) {
    console.error('用法: --client_id=.. --client_secret=.. --identifier=项目标识 [--industry=汽车电子] [--min=2] [--max=12] [--dry]');
    process.exit(1);
  }
  const { token } = await api.getToken(args.client_id, args.client_secret);
  const proj = (await api.listProjects(token)).find(p => p.identifier === args.identifier);
  if (!proj) { console.error('未找到项目', args.identifier); process.exit(1); }
  const items = await api.listWorkItems(token, proj.id);
  console.log(`[OK] 项目 ${args.identifier}, 工作项 ${items.length}, 行业 ${args.industry || '(未指定,用通用词典)'}`);

  const dict = buildDictionary(args.industry);
  const tagNames = L.extractTags(items, dict, { min: MIN, max: MAX });
  console.log(`抽出标签(${tagNames.length}): ${tagNames.join(' / ') || '(无,降低--min试试)'}`);
  if (!tagNames.length) return;

  // 建标签(幂等): 已存在同名复用
  const existing = await api.listTags(token, proj.id);
  const byName = {}; existing.forEach(t => byName[t.name] = t.id);
  let created = 0;
  for (const name of tagNames) {
    if (byName[name]) continue;
    if (DRY) { byName[name] = 'DRY'; created++; continue; }
    const t = await api.createTag(token, { name, projectId: proj.id, color: '#1890ff' });
    byName[name] = t.id; created++; await api.sleep(120);
  }
  console.log(`标签: 新建 ${created}, 复用 ${tagNames.length - created}`);

  // 贴标(并发, 幂等跳过已贴)
  const jobs = [];
  for (const it of items) {
    const has = new Set((it.tags || []).map(t => t.name));
    for (const name of L.matchTags(it, tagNames)) {
      if (!has.has(name)) jobs.push({ wid: it.id, name, tagId: byName[name] });
    }
  }
  let done = 0, fail = 0;
  if (!DRY) {
    const r = await api.batchCreateParallel(jobs, j => api.addWorkItemTag(token, j.wid, j.tagId), { concurrency: 10 });
    done = r.ok; fail = r.fail;
  } else done = jobs.length;

  const tagged = new Set(jobs.map(j => j.wid)).size;
  console.log(`\n=== ${DRY ? '(预览) ' : ''}贴标 ${done}/${jobs.length} 条, 覆盖 ${tagged}/${items.length} 工作项 (${Math.round(tagged / items.length * 100)}%)${fail ? ', 失败 ' + fail : ''} ===`);
  // 每标签命中数
  const per = {}; jobs.forEach(j => per[j.name] = (per[j.name] || 0) + 1);
  Object.entries(per).sort((a, b) => b[1] - a[1]).forEach(([n, c]) => console.log(`  ${n}: ${c}`));
})().catch(e => { console.error('FATAL', e.message); process.exit(1); });
````

- [ ] **Step 2: --dry 预览（不写入）**

Run: `cd scripts && node pingcode_tag_apply.js --client_id=$PINGCODE_CLIENT_ID --client_secret=$PINGCODE_CLIENT_SECRET --identifier=JLALED --industry=汽车电子 --dry`
Expected: 打印抽出的标签（含 配光/ADB/功能安全/诊断/UDS 等领域词）、预览贴标条数与覆盖率，**不写入 PingCode**。

- [ ] **Step 3: 实跑**

Run: `cd scripts && node pingcode_tag_apply.js --client_id=$PINGCODE_CLIENT_ID --client_secret=$PINGCODE_CLIENT_SECRET --identifier=JLALED --industry=汽车电子`
Expected: 标签新建若干、贴标完成、覆盖率打印；PingCode JLALED 工作项上出现对应标签。

- [ ] **Step 4: 幂等复跑**

Run: 同 Step 3 再跑一次
Expected: `标签: 新建 0, 复用 N`，贴标 `0/0`（已全贴），无重复。

- [ ] **Step 5: 提交**

```bash
git add scripts/pingcode_tag_apply.js
git commit -m "feat(tags): pingcode_tag_apply 编排CLI(抽→建→并发贴, 幂等+dry+报告)"
```

---

## Task 5: 文档 + 端到端自检

**Files:**
- Modify: `references/api.md`（标签 API）
- Modify: `skill.md`（资源表）

- [ ] **Step 1: api.md 加标签 API**

在 `references/api.md` 的「关联模块」表后或合适处加：

````markdown
## 标签 (work_item_tags, 2026-06-25 实测)
| 端点 | 方法 | 说明 |
|------|------|------|
| `/v1/project/tags` | POST | 建标签 `{name, project_id, color}` |
| `/v1/project/tags?project_id=` | GET | 列项目标签 |
| `/v1/project/work_items/{id}/tags` | POST | 给工作项贴标签 `{tag_id}`(幂等) |
| `/v1/project/tags/{id}` | DELETE | 删标签 |
> 标签为开放 API 正常能力(无企业鉴权限制)。封装见 `createTag/listTags/addWorkItemTag`。
````

- [ ] **Step 2: skill.md 资源表加脚本**

在 `skill.md` 「参考资源」表中 `pingcode_tags.js` 行后加：

````markdown
| `scripts/pingcode_tag_logic.js` | 标签纯逻辑（词典锚定抽词 extractTags + 关键词匹配 matchTags） |
| `scripts/pingcode_tag_apply.js` | 标签自动打标 CLI（扫已建项目→抽~12标签→并发贴, 幂等+dry+报告） |
````

- [ ] **Step 3: 跑全部单测 + 校验**

Run: `cd scripts && node --test test_tag_logic.js`
Expected: 6 tests PASS。

- [ ] **Step 4: 提交**

```bash
git add references/api.md skill.md
git commit -m "docs(tags): api.md 标签端点 + skill 资源表"
```

---

## 自检（对照 spec）
- **spec 覆盖**：§4架构→Task4；§5抽词+匹配→Task2/3；§6标签API→Task1(已实测,无占位);§7幂等/报告→Task4;§8测试→Task2/3单测+Task4实跑;§9 MVP/dry→Task4。✅ 全覆盖。
- **占位符**：无 TBD；端点均实测真值；每步含完整代码/命令/预期。
- **类型一致**：`extractTags/matchTags/norm/hayOf`、`createTag/listTags/addWorkItemTag`、`buildDictionary/verticalTerms` 跨 Task 命名一致；exports 累进。
- **YAGNI**：别名表去掉（子串 subsume）；不做 key=value/自定义字段/LLM。
